# Geocoding API Configuration
# The scraper uses a fallback chain: Nominatim (free) -> Google Maps (paid)

# Google Maps Geocoding API Key
# Get your API key from: https://console.cloud.google.com/google/maps-apis/credentials
# Required for fallback geocoding when Nominatim fails
# GOOGLE_MAPS_API_KEY=your_key_here

# Enable/disable Google Maps fallback geocoding (default: true if key is set)
# GEOCODING_GOOGLE_ENABLED=true

# Concurrency settings for geocoding
# Nominatim: Limited to 1 request/second per OSM policy (do not change)
# GEOCODING_NOMINATIM_CONCURRENCY=1

# Google: Higher concurrency allowed with paid API
# GEOCODING_GOOGLE_CONCURRENCY=10

# Scraper Configuration
REQUESTS_DELAY=2.0  # Seconds between requests (be respectful!)
